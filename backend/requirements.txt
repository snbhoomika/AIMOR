# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
motor>=3.3.0
pymongo>=4.6.0
beanie>=1.25.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Image Processing
Pillow==10.2.0
python-magic==0.4.27

# ML Integration (for future)
numpy==1.26.3
# torch==2.1.2  # Uncomment when ML integration is ready
# torchvision==0.16.2  # Uncomment when ML integration is ready

# Utilities
pydantic[email]==2.5.3
email-validator==2.1.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.26.0

# CORS (FastAPI has built-in CORS support)

# Redis (for caching - optional)
# redis==5.0.1

# Email (for notifications - optional)
# fastapi-mail==1.4.1
